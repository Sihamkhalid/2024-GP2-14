import 'package:flutter/material.dart';

class Styles {
  static ThemeData themeData() {
    return ThemeData(
      fontFamily: 'SF Pro', // Set the default font family for the entire app
      primarySwatch: Colors.blue, // Set the primary color swatch
    );
  }
}
